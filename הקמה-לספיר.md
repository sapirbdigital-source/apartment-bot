# הקמה — צ'ק ליסט לספיר

המדריך הזה מיועד **לספיר**, ומקים את הכל **על החשבונות שלה בלבד**.
אין שום תלות בחשבון של מישהו אחר.

זמן משוער: כשעה. אין צורך בידע בתכנות — רק להעתיק ולהדביק.

---

## חשבונות שצריך לפתוח (הכל חינם)

| # | שירות | בשביל מה | קישור |
|---|---|---|---|
| 1 | **GitHub** | הבוט רץ שם, כל בוקר, בלי שהמחשב יהיה דלוק | [github.com/signup](https://github.com/signup) |
| 2 | **Apify** | שולף את הפוסטים מקבוצות פייסבוק | כבר נפתח ✅ |
| 3 | **Google AI Studio** | ה-AI שמסנן את המודעות | [aistudio.google.com](https://aistudio.google.com/app/apikey) |
| 4 | **Groq** | AI גיבוי, למקרה שהראשון נופל | [console.groq.com](https://console.groq.com/keys) |
| 5 | **מייל** | לשם מגיע הדייג'סט היומי. אפשר Gmail קיים | — |
| 6 | **פייסבוק** | החשבון הקיים שלה — צריך להיות **חבר בקבוצות** | — |

> ⚠️ **סעיף 6 הוא הקריטי** — אבל לא מהסיבה שנדמה. הסורק **לא משתמש בחשבון
> שלך בכלל**, ולכן הוא רואה רק קבוצות **ציבוריות**; חברות בקבוצה סגורה
> לא עוזרת לו. ראי את הבדיקה בשלב 1.

---

## שלב 1 — להצטרף לקבוצות בפייסבוק

חפשי בפייסבוק והצטרפי (ככל שיותר — יותר תוצאות):

- דירות להשכרה מבשרת ציון
- מבשרת ציון · לוח מודעות / מה קורה במבשרת
- דירות להשכרה בירושלים והסביבה
- השכרת דירות הרי יהודה
- לוח מודעות צור הדסה
- דירות להשכרה בית שמש והסביבה
- כל קבוצה יישובית רלוונטית (אבו גוש, בית זית, מוצא, הר אדר...)

**שמרי את הקישור של כל קבוצה** (הכתובת מהדפדפן) — נצטרך אותם בשלב 3.

### ⚠️ החלק הכי חשוב במדריך: ציבורית או סגורה

הסורק **רואה רק קבוצות ציבוריות**. זו מגבלה של Apify, לא באג — סריקת
קבוצות סגורות דורשת פרטי התחברות, וזה מנוגד לתנאי השימוש של פייסבוק
ועלול להביא לחסימת החשבון שלך. לא שווה את זה.

**איך בודקים, 10 שניות לקבוצה:**

1. פתחי חלון **גלישה בסתר** (`Ctrl+Shift+N`) — כך את לא מחוברת לפייסבוק.
2. הדביקי את קישור הקבוצה.
3. **רואה פוסטים** → ציבורית ✅ שימי אותה ברשימה
   **רואה רק "הצטרפי לקבוצה"** → סגורה ❌ לא תעבוד

עשי את הבדיקה על כל הקבוצות **לפני** שממשיכים לשלב 3. אם רוב הקבוצות
באזור סגורות, עדיף לדעת עכשיו ולחשוב על מקור אחר (למשל יד2) מאשר
לגלות את זה אחרי שהוקם הכל.

> להיות חברה בקבוצה עדיין שווה — כך את רואה מודעות בעצמך גם ממה
> שהבוט לא מגיע אליו.

---

## שלב 2 — להעלות את הקוד לגיטהאב

1. פתחי חשבון ב-[github.com](https://github.com/signup).
2. לחצי על **+** בפינה הימנית העליונה → **New repository**.
3. שם: `apartment-bot`. סמני **Private**. צרי.
4. במסך שנפתח לחצי **uploading an existing file**.
5. **גררי לשם את כל התיקייה** שקיבלת (כולל התיקיות `.github` ו-`data`).
6. לחצי **Commit changes**.

> אם הגרירה מפספסת את התיקייה `.github` — היא מוסתרת בחלק מהמערכות.
> אפשר להעלות אותה בנפרד: **Add file → Create new file**, ובשם הקובץ לכתוב
> `.github/workflows/main.yml` ולהדביק את התוכן.

---

## שלב 3 — Apify (הסורק של פייסבוק)

1. היכנסי ל-[console.apify.com](https://console.apify.com/).
2. **Store** → חפשי `facebook-groups-scraper` → בחרי את זה של **apify**
   (הרשמי, עם סימן האימות).
3. **Try for free** → נפתח מסך ההרצה.
4. למעלה מימין **⋮** → **Create task**.
   ⚠️ חובה **Task** ולא רק Run — הבוט מריץ Task, ורק לו יש ID שעובד.
5. מלאי את השדות:

   | שדה | ערך | למה |
   |---|---|---|
   | `startUrls` | קישורי הקבוצות **הציבוריות** משלב 1, אחד בשורה | |
   | `resultsLimit` | `25` | תקרה לכל קבוצה |
   | `viewOption` | `CHRONOLOGICAL` | לפי זמן, לא לפי "רלוונטיות" |
   | `onlyPostsNewerThan` | `1 days` | **החוסך הכי גדול** — לא משלמים על פוסטים ישנים |

6. **Save** → לחצי **Start** להרצה ידנית אחת, ובלשונית **Output** ודאי
   שחוזרים פוסטים עם טקסט בעברית. אם חוזר ריק — הקבוצות כנראה סגורות.
7. **ה-Task ID**: מהכתובת בדפדפן —
   `console.apify.com/actors/tasks/`**`aBcD1234efGh`** ← החלק המודגש.
8. **ה-Token**: תפריט שמאלי למטה → **Settings** → **API & Integrations** →
   העתיקי את ה-*Personal API token*.

### כמה זה עולה

התוכנית החינמית נותנת **$5 קרדיט לחודש** (לא מצטבר). האקטור הרשמי גובה
**$0.005 לפוסט** — כלומר כ-1,000 פוסטים בחודש בחינם.

6 קבוצות × ~8 פוסטים חדשים ביום ≈ 1,440 פוסטים בחודש ≈ **$7.2** — קצת מעל החינמי.

לכן הבוט רץ **פעם ביום** ולא פעמיים: עם פילטר של 24 שעות, הרצה שנייה
סורקת שוב את אותו חלון ומשלמת עליו פעמיים.

אם חורגים — יש אקטורים זולים יותר שמחזירים בדיוק את אותם שדות, למשל
`memo23/facebook-public-group-posts-scraper` ב-$0.0015 לפוסט (פי 3.3 יותר
בשביל אותו כסף). ההחלפה היא רק עדכון של `APIFY_TASK_ID_1` — בלי שינוי בקוד.

---

## שלב 4 — ערוץ ההתראות (מייל)

הבוט רץ פעם ביום ושולח **מייל אחד מרוכז** עם כל הדירות שנמצאו.
אין אפליקציה להתקין ואין סשן שמתנתק.

1. בחשבון הגוגל שלך צריך **אימות דו-שלבי** פעיל
   ([myaccount.google.com/security](https://myaccount.google.com/security)).
2. היכנסי ל-[myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords).
3. תני שם כלשהו (למשל `apartment bot`) → **Create**.
4. יוצג קוד בן **16 תווים** — העתיקי אותו. הוא לא יוצג שוב.

> ⚠️ זו **לא** סיסמת החשבון שלך. סיסמת חשבון רגילה תיכשל, ובכוונה —
> App Password אפשר לבטל בנפרד בלי לגעת בסיסמה שלך.

שלושת הערכים שצריך:

| | |
|---|---|
| `SMTP_USER` | כתובת הג'ימייל שלך |
| `SMTP_PASSWORD` | הקוד בן 16 התווים |
| `EMAIL_TO` | לאן לשלוח — אפשר אותה כתובת |

**רוצה גם פוש לנייד?** (אופציונלי) התקיני את אפליקציית **ntfy**,
הירשמי לנושא בשם ארוך ואקראי (למשל `sapir-dirot-9f3k2xq7`), והגדירי
`NTFY_TOPIC` לאותו שם. בלי חשבון ובלי מפתח.

---

## שלב 5 — מפתחות ה-AI

**Gemini (ראשי):**
1. [aistudio.google.com/app/apikey](https://aistudio.google.com/app/apikey) עם חשבון הגוגל שלך.
2. **Create API key** → העתיקי (מתחיל ב-`AIza`).

**Groq (גיבוי):**
1. [console.groq.com/keys](https://console.groq.com/keys) → הרשמה.
2. **Create API Key** → העתיקי (מתחיל ב-`gsk_`).

---

## שלב 6 — להכניס את הכל לגיטהאב

בריפו שיצרת: **Settings** (בסרגל העליון של הריפו) → בתפריט השמאלי
**Secrets and variables** → **Actions** → **New repository secret**.

הוסיפי אחד-אחד. השם חייב להיות **בדיוק** כך:

| Name | Value | חובה |
|---|---|---|
| `APIFY_TOKEN_1` | הטוקן משלב 3 | ✅ |
| `APIFY_TASK_ID_1` | ה-Task ID משלב 3 | ✅ |
| `GEMINI_API_KEY` | המפתח משלב 5 | ✅ |
| `GROQ_API_KEY` | המפתח משלב 5 | מומלץ |
| `SMTP_USER` | הג'ימייל שלך | ✅ |
| `SMTP_PASSWORD` | ה-App Password בן 16 התווים | ✅ |
| `EMAIL_TO` | לאן לשלוח | ✅ |
| `NTFY_TOPIC` | שם הנושא, אם הגדרת פוש | אופציונלי |

---

## שלב 7 — בדיקה שההתראות מגיעות

**לפני** שמריצים את הבוט המלא, כדאי לוודא שההודעות בכלל מגיעות.
ב-Actions יש לזה הרצה נפרדת: **Notification Test** → **Run workflow**.

תוך דקה אמור להגיע מייל בדיקה עם שתי דירות דמה (גם לספאם שווה להציץ). אם היא לא מגיעה — אין טעם
להמשיך, כי כל השאר יעבוד והמודעות פשוט ילכו לאיבוד. בדקי שהערכים של המייל הוזנו נכון.

---

## שלב 8 — הפעלה ראשונה

1. בריפו → לשונית **Actions**.
2. אם מופיעה הודעה כחולה עם כפתור **I understand my workflows, go ahead and enable them** — לחצי עליו.
3. בתפריט השמאלי: **Sapir Apartment Bot** → מימין **Run workflow** → **Run workflow**.
4. חכי כמה דקות ורענני. לחצי על ההרצה כדי לראות את הלוג.

**מה אמור לקרות:** בלוג תראי `ערוצים מוגדרים: email`, אחר כך `מפעיל Apify`,
מספר הפוסטים, ואז שורות `✅ נשלח` — ויגיע מייל אחד עם כל מה שנמצא.

מכאן הוא רץ לבד כל בוקר בשעה 8, ומביא את כל מה שהתפרסם ב-24 השעות שקדמו.

---

## אם משהו לא עובד

| מה רואים בלוג | מה זה אומר | מה לעשות |
|---|---|---|
| `אין אף ערוץ שליחה מוגדר` | שמות ה-Secrets לא מדויקים | לבדוק אותיות גדולות/קטנות |
| `התחברות נדחתה — צריך App Password` | הוזנה סיסמת החשבון | ליצור App Password |
| הבוט אומר `✅ נשלח` אבל אין מייל | הגיע לספאם | לסמן "לא ספאם" פעם אחת |
| `קבוצה לא נגישה` | הקבוצה סגורה בפייסבוק | להחליף לקבוצה ציבורית |
| `כל חשבונות ה-Apify מוצו` | הקרדיט נגמר או ה-Task לא מוגדר נכון | בדקי ב-Apify שה-Task רץ ידנית ומחזיר תוצאות |
| `0 תוצאות` | החשבון לא חבר בקבוצות, או שהן ריקות | לוודא חברות בקבוצות |
| הכל רץ אבל `נדחו 40` | הסינון מחמיר מדי לשוק | ראי "התקציב" למטה |
| `❌ הניתוח נכשל` | מפתח ה-AI לא תקין | ליצור מפתח חדש |

---

## על התקציב — קראי את זה

4,000 ₪ למשפחה עם שני ילדים במבשרת זה **תקציב הדוק**. במבשרת עצמה דירת
3 חדרים היא בערך 5,500–7,000 ₪. ריאלי למצוא ב-4,000 בעיקר **יחידות דיור**
או דירות ביישובים שמסביב.

לכן הבוט לא חוסם הכל בקשיחות:
- עד 4,000 ₪ — עובר נקי
- 4,000–4,500 ₪ — עובר עם דגל ⚠️
- מעל 4,500 ₪ — נחסם

אם הוואטסאפ שקט מדי, אפשר לפתוח את זה: בקובץ `config.py` בריפו (לחצי עליו →
אייקון העיפרון ✏️), לשנות את השורה `BUDGET_STRETCH = 4500` למספר גבוה יותר,
ולשמור (**Commit changes**). זה נכנס לתוקף בהרצה הבאה.

באותו קובץ אפשר גם לשנות אזורים (`CORE_AREAS`), מינימום חדרים (`MIN_ROOMS`)
ותקציב (`BUDGET`).
