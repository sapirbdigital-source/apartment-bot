# הקמה — צ'ק ליסט לספיר

המדריך הזה מיועד **לספיר**, ומקים את הכל **על החשבונות שלה בלבד**.
אין שום תלות בחשבון של מישהו אחר.

זמן משוער: כשעה. אין צורך בידע בתכנות — רק להעתיק ולהדביק.

---

## חשבונות שצריך לפתוח (הכל חינם)

| # | שירות | בשביל מה | קישור |
|---|---|---|---|
| 1 | **GitHub** | הבוט רץ שם, פעמיים ביום, בלי שהמחשב יהיה דלוק | [github.com/signup](https://github.com/signup) |
| 2 | **Apify** | שולף את הפוסטים מקבוצות פייסבוק | כבר נפתח ✅ |
| 3 | **Google AI Studio** | ה-AI שמסנן את המודעות | [aistudio.google.com](https://aistudio.google.com/app/apikey) |
| 4 | **Groq** | AI גיבוי, למקרה שהראשון נופל | [console.groq.com](https://console.groq.com/keys) |
| 5 | **Green API** | שולח את ההתראות לוואטסאפ שלך | [green-api.com](https://green-api.com/) |
| 6 | **פייסבוק** | החשבון הקיים שלה — צריך להיות **חבר בקבוצות** | — |

> ⚠️ **סעיף 6 הוא הקריטי.** סורק לא רואה קבוצות סגורות שהחשבון לא חבר בהן.
> לפני הכל — להצטרף לקבוצות ולחכות לאישור. זה יכול לקחת יום-יומיים.

---

## שלב 1 — להצטרף לקבוצות בפייסבוק

חפשי בפייסבוק והצטרפי (ככל שיותר — יותר תוצאות):

- דירות להשכרה מבשרת ציון
- מבשרת ציון · לוח מודעות / מה קורה במבשרת
- דירות להשכרה בירושלים והסביבה
- השכרת דירות הרי יהודה
- לוח מודעות צור הדסה
- דירות להשכרה בית שמש והסביבה
- כל קבוצה יישובית רלוונטית (אבו גוש, בית זית, מוצא, הר אדר...)

**שמרי את הקישור של כל קבוצה** (הכתובת מהדפדפן) — נצטרך אותם בשלב 3.

---

## שלב 2 — להעלות את הקוד לגיטהאב

1. פתחי חשבון ב-[github.com](https://github.com/signup).
2. לחצי על **+** בפינה הימנית העליונה → **New repository**.
3. שם: `apartment-bot`. סמני **Private**. צרי.
4. במסך שנפתח לחצי **uploading an existing file**.
5. **גררי לשם את כל התיקייה** שקיבלת (כולל התיקיות `.github` ו-`data`).
6. לחצי **Commit changes**.

> אם הגרירה מפספסת את התיקייה `.github` — היא מוסתרת בחלק מהמערכות.
> אפשר להעלות אותה בנפרד: **Add file → Create new file**, ובשם הקובץ לכתוב
> `.github/workflows/main.yml` ולהדביק את התוכן.

---

## שלב 3 — Apify (הסורק של פייסבוק)

1. היכנסי ל-[console.apify.com](https://console.apify.com/).
2. **Store** → חפשי `Facebook Groups Scraper` → **Try for free**.
3. למעלה מימין: **Create task** (חשוב — Task, לא רק Run).
4. בשדה `startUrls` הדביקי את קישורי הקבוצות מ**שלב 1**, אחד בכל שורה.
5. `resultsLimit`: **50** לכל קבוצה. אל תגדילי — הקרדיט נגמר מהר.
6. אם יש אפשרות מיון — בחרי **New posts / Most recent**.
7. **Save**.
8. **ה-Task ID**: הסתכלי על הכתובת בדפדפן. היא נראית כך —
   `console.apify.com/actors/tasks/`**`aBcD1234efGh`** — החלק המודגש הוא ה-ID. שמרי אותו.
9. **ה-Token**: תפריט ימני למטה → **Settings** → **API & Integrations** → העתיקי את
   ה-*Personal API token*. שמרי אותו.

---

## שלב 4 — וואטסאפ (Green API)

Green API הוא שירות שמחבר את הוואטסאפ שלך למערכות אוטומטיות. ההתראות
יגיעו אלייך כהודעת וואטסאפ רגילה.

1. הירשמי ב-[green-api.com](https://green-api.com/) (תוכנית **Developer** — חינם).
2. ב-Console לחצי **Create an instance** → בחרי **Developer**.
3. יופיעו לך שני ערכים — **העתיקי את שניהם**:
   - `idInstance` — מספר בן 10 ספרות (למשל `1101234567`)
   - `apiTokenInstance` — מחרוזת ארוכה
4. **חיבור הוואטסאפ:** לחצי על האינסטנס → לשונית **QR**.
   בטלפון: וואטסאפ → ⚙️ הגדרות → **מכשירים מקושרים** → **קישור מכשיר** →
   סרקי את הקוד.
5. רענני. הסטטוס צריך להשתנות ל-**`authorized`** (ירוק). אם הוא `notAuthorized` —
   הסריקה לא תפסה, נסי שוב.
6. שימי לב אם מופיעה כתובת ייעודית כמו `https://7101.api.greenapi.com` —
   אם כן, שמרי גם אותה.

**את מספר הטלפון שלך** צריך בפורמט `972501234567` (בלי 0 בהתחלה, בלי מקפים).
אפשר גם לכתוב `0501234567` — הקוד ממיר לבד.

> ⚠️ **חשוב לדעת:** החיבור הזה תלוי בכך שהטלפון שלך מחובר לאינטרנט מדי פעם.
> אם הוא כבוי ימים ארוכים, וואטסאפ מנתק את המכשיר המקושר וההתראות ייפסקו.
> אם זה קורה — פשוט סרקי QR מחדש. הבוט מדווח על זה בלוג.

---

## שלב 5 — מפתחות ה-AI

**Gemini (ראשי):**
1. [aistudio.google.com/app/apikey](https://aistudio.google.com/app/apikey) עם חשבון הגוגל שלך.
2. **Create API key** → העתיקי (מתחיל ב-`AIza`).

**Groq (גיבוי):**
1. [console.groq.com/keys](https://console.groq.com/keys) → הרשמה.
2. **Create API Key** → העתיקי (מתחיל ב-`gsk_`).

---

## שלב 6 — להכניס את הכל לגיטהאב

בריפו שיצרת: **Settings** (בסרגל העליון של הריפו) → בתפריט השמאלי
**Secrets and variables** → **Actions** → **New repository secret**.

הוסיפי אחד-אחד. השם חייב להיות **בדיוק** כך:

| Name | Value | חובה |
|---|---|---|
| `APIFY_TOKEN_1` | הטוקן משלב 3 | ✅ |
| `APIFY_TASK_ID_1` | ה-Task ID משלב 3 | ✅ |
| `GEMINI_API_KEY` | המפתח משלב 5 | ✅ |
| `GROQ_API_KEY` | המפתח משלב 5 | מומלץ |
| `WHATSAPP_TO` | מספר הטלפון שלך, `972501234567` | ✅ |
| `GREEN_API_INSTANCE_ID` | ה-`idInstance` משלב 4 | ✅ |
| `GREEN_API_TOKEN` | ה-`apiTokenInstance` משלב 4 | ✅ |
| `GREEN_API_URL` | הכתובת הייעודית, אם קיבלת כזו | רק אם יש |

---

## שלב 7 — בדיקה שהוואטסאפ מחובר

**לפני** שמריצים את הבוט המלא, כדאי לוודא שההודעות בכלל מגיעות.
ב-Actions יש לזה הרצה נפרדת: **WhatsApp Test** → **Run workflow**.

תוך דקה אמורה להגיע לוואטסאפ הודעת בדיקה. אם היא לא מגיעה — אין טעם
להמשיך, כי כל השאר יעבוד והמודעות פשוט ילכו לאיבוד. בדקי ב-Green API
שהסטטוס `authorized`.

---

## שלב 8 — הפעלה ראשונה

1. בריפו → לשונית **Actions**.
2. אם מופיעה הודעה כחולה עם כפתור **I understand my workflows, go ahead and enable them** — לחצי עליו.
3. בתפריט השמאלי: **Sapir Apartment Bot** → מימין **Run workflow** → **Run workflow**.
4. חכי כמה דקות ורענני. לחצי על ההרצה כדי לראות את הלוג.

**מה אמור לקרות:** בלוג תראי `ערוצי שליחה זמינים: green`, אחר כך `מפעיל Apify`,
מספר הפוסטים, ואז שורות `✅ נשלח` — ובוואטסאפ יגיעו ההתראות.

מכאן הוא רץ לבד פעמיים ביום, ב-8 בבוקר וב-6 בערב.

---

## אם משהו לא עובד

| מה רואים בלוג | מה זה אומר | מה לעשות |
|---|---|---|
| `אין אף ערוץ שליחה מוגדר` | שמות ה-Secrets לא מדויקים | לבדוק אותיות גדולות/קטנות |
| `green נכשל: קוד 401` | ה-token או ה-instance id שגויים | להעתיק מחדש מ-Green API |
| `green נכשל: קוד 466` | נגמרה המכסה החודשית של Green | לחכות לחודש הבא או לשדרג |
| הבוט אומר `✅ נשלח` אבל לא הגיע כלום | הוואטסאפ התנתק מהאינסטנס | ב-Green API: לבדוק שהסטטוס `authorized`, אם לא — לסרוק QR מחדש |
| `כל חשבונות ה-Apify מוצו` | הקרדיט נגמר או ה-Task לא מוגדר נכון | בדקי ב-Apify שה-Task רץ ידנית ומחזיר תוצאות |
| `0 תוצאות` | החשבון לא חבר בקבוצות, או שהן ריקות | לוודא חברות בקבוצות |
| הכל רץ אבל `נדחו 40` | הסינון מחמיר מדי לשוק | ראי "התקציב" למטה |
| `❌ הניתוח נכשל` | מפתח ה-AI לא תקין | ליצור מפתח חדש |

---

## על התקציב — קראי את זה

4,000 ₪ למשפחה עם שני ילדים במבשרת זה **תקציב הדוק**. במבשרת עצמה דירת
3 חדרים היא בערך 5,500–7,000 ₪. ריאלי למצוא ב-4,000 בעיקר **יחידות דיור**
או דירות ביישובים שמסביב.

לכן הבוט לא חוסם הכל בקשיחות:
- עד 4,000 ₪ — עובר נקי
- 4,000–4,500 ₪ — עובר עם דגל ⚠️
- מעל 4,500 ₪ — נחסם

אם הוואטסאפ שקט מדי, אפשר לפתוח את זה: בקובץ `config.py` בריפו (לחצי עליו →
אייקון העיפרון ✏️), לשנות את השורה `BUDGET_STRETCH = 4500` למספר גבוה יותר,
ולשמור (**Commit changes**). זה נכנס לתוקף בהרצה הבאה.

באותו קובץ אפשר גם לשנות אזורים (`CORE_AREAS`), מינימום חדרים (`MIN_ROOMS`)
ותקציב (`BUDGET`).
